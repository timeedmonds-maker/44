name: Extract Stats Events resolver modules
on:
  push:
    branches: [codex/adams-westbrook-hls-final]
    paths:
      - analysis/extract_stats_events_resolver_modules.py
      - .github/workflows/extract-stats-events-resolver-modules.yml
  workflow_dispatch:
jobs:
  extract:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install curl_cffi
      - run: python -u analysis/extract_stats_events_resolver_modules.py
