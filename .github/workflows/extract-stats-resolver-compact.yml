name: Extract compact NBA Stats video resolver
on:
  push:
    branches: [codex/adams-westbrook-hls-final]
    paths:
      - analysis/extract_stats_resolver_compact.py
      - .github/workflows/extract-stats-resolver-compact.yml
  workflow_dispatch:
jobs:
  extract:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install curl_cffi
      - run: python -u analysis/extract_stats_resolver_compact.py
