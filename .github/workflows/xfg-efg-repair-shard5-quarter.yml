name: xFG eFG Repair Shard 5 Quarter

on:
  push:
    paths:
      - '.github/workflows/xfg-efg-repair-shard5-quarter.yml'
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  repair:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        shard: [5,25]
    steps:
      - name: Fetch shard script
        run: |
          set -euo pipefail
          curl --retry 3 --retry-delay 1 -fsSL \
            "https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/${GITHUB_SHA}/xfg_efg_shard.py" \
            -o xfg_efg_shard.py
      - name: Install dependencies
        run: python3 -m pip install --quiet --disable-pip-version-check pandas pyreadr requests
      - name: Extract quarter repair
        run: |
          python3 xfg_efg_shard.py \
            --shard-index ${{ matrix.shard }} \
            --num-shards 40 \
            --workers 2 \
            --min-fga 300 \
            --out-dir xfg_efg_quarter
      - name: Upload quarter repair
        uses: actions/upload-artifact@v4
        with:
          name: xfg-efg-quarter-${{ matrix.shard }}
          path: |
            xfg_efg_quarter/partial_${{ matrix.shard }}.csv
            xfg_efg_quarter/errors_${{ matrix.shard }}.csv
            xfg_efg_quarter/summary_${{ matrix.shard }}.json
          if-no-files-found: error
          retention-days: 1
          compression-level: 0
