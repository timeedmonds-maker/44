name: Adams Jazz v32u adaptive epipolar-trimmed MHR solve

on:
  push:
    branches: [codex/multi-angle-freeze-spin-poc]
    paths:
      - 'freeze_spin/run_v32u_epipolar_trimmed_broadcast_mhr.py'
      - '.github/workflows/adams-jazz-v32u-epipolar-trimmed-mhr.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  epipolar-trimmed-mhr:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      PYTHONUNBUFFERED: '1'
      PYTHONFAULTHANDLER: '1'
      OMP_NUM_THREADS: '4'
      MKL_NUM_THREADS: '4'
    steps:
      - uses: actions/checkout@v4
      - name: Install observation and MHR runtime
        run: |
          set -euxo pipefail
          python -m pip install --quiet numpy scipy opencv-python-headless pillow 'rfdetr>=1.8.1' 'pymomentum-core>=0.1.111' 'git+https://github.com/facebookresearch/MHR.git'
      - name: Download exact B32 source package
        uses: actions/download-artifact@v4
        with:
          name: adams-jazz-v32-sharp-latent-quality-cluster-4dgs-34669584507
          path: inputs_b32
          github-token: ${{ github.token }}
          repository: timeedmonds-maker/44
          run-id: 34669584507
      - name: Run v32u trimmed multiview solve
        shell: bash
        run: |
          set +e
          mkdir -p v32u work_v32u
          /usr/bin/time -v python -u -X faulthandler -m freeze_spin.run_v32u_epipolar_trimmed_broadcast_mhr \
            --b32-root inputs_b32 --work work_v32u --out v32u --max-nfev 340 2>&1 | tee v32u/run.log
          rc=${PIPESTATUS[0]}
          echo "$rc" > v32u/solver_exit_code.txt
          exit 0
      - name: Print v32u diagnostic
        if: always()
        run: |
          python - <<'PY'
          import json,pathlib
          for n in ('v32u_epipolar_trimmed_audit.json','v32u_qa.json'):
              p=pathlib.Path('v32u')/n
              if not p.exists(): continue
              q=json.load(open(p)); print(n)
              if n.startswith('v32u_epipolar'):
                  keys=['retained_body_joint_count','retained_body_joints','retained_epi_median_px','retained_epi_max_px','selected_source_counts','all_surviving_best_epi_px']
              else:
                  keys=['status','per_camera_reprojection','left_best_validation','gate','v32u_base_exit_code']
              print(json.dumps({k:q.get(k) for k in keys},indent=2))
          PY
      - name: Package v32u evidence
        if: always()
        run: |
          set -euxo pipefail
          mkdir -p delivery
          cp v32u/* delivery/ 2>/dev/null || true
          rm -f delivery/*.zip
          test -n "$(find delivery -maxdepth 1 -type f -print -quit)"
          (cd delivery && zip -0 adams_jazz_v32u_epipolar_trimmed_mhr.zip ./*)
      - name: Upload v32u evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adams-jazz-v32u-epipolar-trimmed-mhr-${{ github.run_id }}
          path: delivery/
          retention-days: 14
          if-no-files-found: error
          compression-level: 0
      - name: Enforce fail-closed v32u gate
        if: always()
        run: |
          set -euxo pipefail
          test "$(cat v32u/solver_exit_code.txt 2>/dev/null || echo 99)" = "0"
